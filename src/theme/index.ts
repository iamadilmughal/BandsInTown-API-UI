import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: "#00CEC8",
        },
        secondary: {
            main: "#D2635F",
        },
    },
});

export default theme;
